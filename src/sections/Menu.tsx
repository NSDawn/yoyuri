import "./Menu.css"
import { useGlobal } from "../GlobalContextHandler";

export default function Menu() {
    
    const G = useGlobal();

    return (
        <section className="menu">

        </section>
    );
}